calendar.html --- the web page dislays the one day calendar

css/calendar.css --- the css file of the one day calendar

img/blueBarLogo.png --- the images needed for calendar the blue bar with facebook logo

js/testInput.js ---  the scripts contains the onload functions that executes when page loaded 
					inside the onload() function it calls the layOutDay function with test inputs
					
   layOutDay.js ---  the implementation of the layOutDay function